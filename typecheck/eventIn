/*
 *   return statement 
 *
 *
 */


    event a (int a1 , int a2);
    event b (double f1, double f2);

    int w, x;
	double y, z;

	a(w, x) -> { x = 1; };
/*	b(y, z) -> { z = 2.0; }; */
	a(x, w) -> { w = 3; };
